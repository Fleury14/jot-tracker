const INITIAL_STATE = { test: '' };

const testReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default testReducer;